# 2026-09-24：推薦の増分・候補生成・配信検証の5件

一次資料に基づく日本語Labを5件追加した。HTMLは各LabのYAML、Markdown、コード、実行結果から生成する。論文PDFの版とSHA-256はmapping.mdに記録し、PDF自体は再配布しない。IGPOのPDFはarXivから取得した。

## 原典を読んで補った条件

| Lab | 著者報告 | 読む際の条件 |
| --- | --- | --- |
| Spotify | 表示−7.1%、消費−0.37%で有意差なし | 計測窓が異なるheadの予測差はCATEではない。本文の閾値方向とEq. 5が不一致のため、実装は式に従う。非劣性を証明したとは扱わない |
| Pinterest | Table 4はonline −64.4%、offline −64.6% | 本番の主方策はSV。Table 5のSuccess Curation +0.26%、Closeup Repins +1.10%と、別群の最大85%削減を結合しない |
| SWE-Serve | 53課題、E2E除去で45.9→69.4% | 報告差23.4ポイントと丸め値の差23.5ポイントを区別。複数domainの差21.3ポイントはdomain数だけの因果効果ではない |
| SAM-D2Q | 21日・4%のA/BでGMV +3.38%、Pay Count +2.27% | GMVは方向を示す値。95%確認はPay CountとCVR。A/Bと報酬ログの記載日付には前後の不一致がある |
| IGPO | CTR相対+3.17%、監査bad case −38.9% | 在庫情報と規則の施策全体。FNIとFMは分母が異なる。標準構成の単一実行と、同一token予算の比較を分ける |

## 人工実験の境界

二重閾値、DRの残差補正、API配線と状態の検査、商品側の語彙拡張、在庫情報を読む固定規則を小さな例で実行した。実ログは使わない。Mechanismは5件ともPARTIALで、個別の検査だけをCONFIRMEDとした。Performance / Scaling / Production applicabilityはNOT TESTED。商用A/BやGPU測定を再現したとは扱わない。

図は論文ごとに選んだ。Spotifyは計測窓の時間図、Pinterestは候補源のgateとDR式、SWE-Serveは課題系列の表と検査範囲の比較、SAM-D2Qは索引作成と検索の経路、IGPOは在庫情報と規則更新の分離を中心にする。操作部分はrun.pyが計算したJSONを表示する。

## 検証

`uv run pytest -q`で170件が通過した。5件のrun.pyを一時ディレクトリへ単体コピーして再実行し、保存済みresults.jsonとの一致も検査する。生成の決定性、出典リンク、成果物の配布を既存テストで確認した。

Chromeでは5件を360・390・768・1440pxで確認し、112の選択状態を検査した。本文の横はみ出し、キーボード操作、内部リンク、原本とdownloadの一致、index掲載、JavaScript無効時の表示も確認済み。図は画像でも確認した。

日本語はnatural-japanese quickで推敲し、`--genre tech`でREADME・手法・mapping・抽出したページ本文と図の説明の計25対象を検査し、指摘0件となった。作業用lint出力と画面画像は公開物に含めない。

## 再実行

```sh
UV_CACHE_DIR=/tmp/uv-cache uv run pytest -q
UV_CACHE_DIR=/tmp/uv-cache uv run paper-lab build
UV_CACHE_DIR=/tmp/uv-cache uv run --with playwright tests/browser_september24.py
UV_CACHE_DIR=/tmp/uv-cache uv run skills/natural-japanese/scripts/lint.py --genre tech --json <日本語本文.md>
```
