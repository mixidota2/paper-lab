# 2026-09-21 Research Labs 検証記録

6本の一次論文を全文HTMLで読み、版とSHA-256を各mapping.mdに記録した。本文は `/tmp/research-2026-09-21/` に一度ずつ保存。指定のtmp-scoutは読み取り専用だったため、許可されたfallbackを使った。

| 優先度 | Lab | 確認した版 | 読み違いを避ける点 |
| --- | --- | --- | --- |
| Must | [LIGE-GR](papers/lige-gr-meta-listwise/README.md) | v1 | +1.14%/+0.72%はbeam 1。FB views −0.52%。60–80%はCA throughputの増加 |
| Must | [Sona](papers/sona-yandex-music/README.md) | v2 | 最終A/Bは各群15%・7日。全流量未展開。実験4との差は履歴の因果効果ではない |
| Worth | [UVR](papers/uvr-wolt-trial-reorder/README.md) | v1 | 国別学習・領域間共有。V3内部には領域別CatBoostが残る |
| Radar | [PushDualGen](papers/pushdualgen-kuaishou/README.md) | v1 | 計15%を等分。push向け。Qwen3-0.6BのSID→任意copy |
| Radar | [SnapLGR](papers/snaplgr-snapchat/README.md) | v3 | Qwen3-0.6B。View Time +0.37%。daily batch生成とonline index取得を区別 |
| Radar | [SIDScope](papers/sidscope-diagnostics/README.md) | v1 | online A/Bなし。D3の関連はprefix利用機構に依存 |

## 実行した検査

- `UV_CACHE_DIR=/tmp/uv-cache uv run pytest -q`: 131件成功。既存catalog期待値を51本へ更新した。
- 6本の `uv run papers/<slug>/run.py`: 結果JSONを生成。追加テストで保存値と再実行値の一致を確認した。
- `uv run paper-lab build`: 51ページ生成。原本は各LabのYAML、Markdown、コード、結果JSON。
- `uv run --with playwright tests/browser_september21.py`: 360 / 390 / 768 / 1440px、6本、36のwidget状態。横はみ出し、目次anchor、ローカルリンク、36個のdownload一致、index掲載、keyboard操作、JavaScript無効時を検査した。
- Chromeの360px/1440pxスクリーンショットを目視確認。UVRの手法名の途中折返しを修正した。長い表は内部を横スクロールする。
- natural-japanese quick: 6本のREADMEとmethodへ `uv run scripts/lint.py --genre tech` を実行。禁止句と否定対比の反復を修正。PushDualGenの文長統計警告は、数値定義と検証範囲を明確にする短文として通読後に保持した。LIGEのmethod内JSONは切替用の文言を繰り返すため、語彙多様性の情報レベル指摘を保持した。
- `git diff --check`: 問題なし。

## 公式SIDScopeを実際に動かした範囲

v1.0.1 releaseを取得し、uvでCPU reviewer quickstartを実行。preflightはpassed、D1–D5のCSVが生成された。12 itemの公開例でleaf数11、D2衝突item率2/12、D5重複SID率1/12、prefix数3/6/11がLabの独立実装と一致した。

[公式実行の数値とhash](papers/sidscope-diagnostics/official-check.json)を保存した。これは公開の小さな入力例で、本番mapや公式全routeの再検証ではない。独立実装は `--mapping` で任意の実JSON対応表を受け取れる。

## 小実験の限界

全Labの総合判定はMechanism PARTIAL、Performance / Scaling / Production applicability NOT TESTED。LIGEはCAを人工値、Sonaは線形student、UVRはpairwise線形代理、PushDualGenは有限頻度モデルへ簡略化した。SnapLGRはSID→item展開の境界だけを検証する。SIDScopeは一部のmap診断とtrace会計で、D4・公式近傍抽出・generator適応は省略した。

各Labは著者のA/B結果とLab実験値を分ける。合成実験から本番lift、GPU throughput、オンライン学習の遅延を再現したとは主張しない。
