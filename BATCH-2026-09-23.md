# 2026-09-23：測定・表現共有・配信検証のLab

優先3本を先行公開し、Radarの4本を後続コミットへ追加した。一次資料は既存の `/workspace/research-bot/tmp-scout-2026-09-23/pdf-<id>.pdf` と抽出本文を再利用し、本文・式・実験条件を読んだ。各Labの `source_review` と `mapping.md` にPDFのSHA-256、版、式・表との対応を記録した。arXivの書誌も確認。PDF自体は再配布していない。

## 確認した事実と、公開時に補った条件

| Lab / 一次資料 | 確認した著者報告 | 留保 |
| --- | --- | --- |
| Netflix 2609.22747 §3–4 / Appendix 6.2 | SNIPS、LOO、Irreplaceability × Universality。11件のrow除去A/BとR²=0.8212。Hájek、clip [0.0001,0.9999]、100観測以上 | 個別座標を作らない。R²は再計算していない。単段LOOの10th percentile下限は別の条件。label誤帰属の監視事例に回復量はない |
| UNIQUE 2609.23718 §3–4 / Tables 3–6 | EQN / UIGN / TDN、flat 16,384 code。1週間A/Bでwatch +0.96%、distribution +1.08%。P99 89 ms、MFU 44.23% | retrieval段への導入。約400 L20、TensorRT FP16、cache条件を併記。導入開始月は§1 Februaryと§4.1.2 Januaryで不一致 |
| FlashVector 2609.17391 Algorithm 1 / Tables 1–4 | Ranking model 2はthroughput 2×、latency 1.98×。feature podは940→約1,500 RPS、約1.6× | Table 4 Model BはSLO 250 ms / p99 271 ms。PDF p.8の画像でも確認。局所30×や350×を全体倍率へ転用しない |

## 先行3本の実行・表示確認

`uv run`で各run.pyを実行。SNIPSは同一の人工ログからfull/LOOを推定し解析的期待値と照合。UNIQUEは距離補正とtarget attentionの局所計算、FlashVectorは人工candidateの採択規則を実行する。すべてMechanism PARTIAL、Performance / Scaling / Production applicability NOT TESTED。局所の不変条件だけをCONFIRMEDとした。

既存suiteと新規検査を合わせ151 testsが通過。最初の失敗はcatalogの期待件数・ID追加漏れと、既存の「一次資料」表記要件で、修正後に全suiteを再実行した。buildの決定性は既存testで検査。Chromeで3本×360/390/768/1440 px、28の選択状態、keyboard、内部anchor、local link、download一致、index掲載、JS無効時の表示を確認した。PCとphoneの図を画像でも確認。

natural-japanese quickを適用し、本文・図・README・mappingをlint。残ったTTR通知はSNIPSやtargetなど同じ概念の一貫した呼び方を保持するため残す。Netflixの「監視が問題発見につながったことは報告されるが…示されない」は機械が離れた節を結合したinfo通知で、主述の不整合はないと判断した。見出しと段落頭、数値条件、著者報告と解釈の分離を通読した。

## 再実行

```bash
UV_CACHE_DIR=/tmp/uv-cache uv run pytest -q
UV_CACHE_DIR=/tmp/uv-cache uv run paper-lab build
UV_CACHE_DIR=/tmp/uv-cache uv run --with playwright tests/browser_september23.py
UV_CACHE_DIR=/tmp/uv-cache uv run skills/natural-japanese/scripts/lint.py --genre tech --json <対象本文.md>
```

既定cacheは読み取り専用のため/tmpへ変更。Playwright取得とChrome起動はsandbox外実行が必要だった。これは配信物に追加の依存を導入するものではない。

## Radar 4本の追加と最終確認

優先3本の先行commitは `0e6b5d08111018357f27e3e8566f5b9fc876dbc0`。指定されたpersonal `mixidota2/paper-lab` のmainへpushし、Pages workflow `35801009836` のsuccessを確認してから最終公開へ進めた。

| Lab / 一次資料 | 確認した著者報告 | 留保 |
| --- | --- | --- |
| YouTube Music 2609.23877 §3–4 / Table 1 | YDD棚の2週間A/B、engagement +22.43% [16.93,27.93]、discovery metric +8.07% [0.90,15.24]。理由だけのholdbackで主なengagement差が消える | 全サービスへの効果ではない。holdback別の数値は未公開。KGの実在確認と理由文の正しさは別。judgeはGemini 3.1 Pro、generatorの版は確定しない |
| MuSeR 2609.23677 Eq. 6 / §III–IV / Fig. 4 | homepage同一A/BでDAU +0.26%、session +0.89%、複数週、下流固定。非圧縮/短期/圧縮のRecall@500は0.2351/0.1749/0.2232 | latency −27%は従来flat HNSW比、絶対msや分位不明。公開Amazonは長さ50であり10万履歴の検証にならない |
| HGenPush 2607.03362 Eq. 14, 22–24 / Tables 3,6 | video SIDと(author₁,author₂,video₁)を生成しrankerへ直結。DAU +0.181% p=0.04、CTR +1.577% p=0.11、duration +0.148% p=0.75 | CTRとdurationを有意差としない。PDF p.9のTable 6画像でも確認。全構成の差分をauthor単独の有意な効果へ変換しない |
| t0 2609.24559 §4.1 / §5.1.2 Table 5 / §6 | 同一alpha checkpoint、既知未来30課題のskill 36.7→43.0、+6.3 pp [2.2,10.9]。19改善・11悪化 | 保存txtは10ページで途切れていた。同じPDF全39ページを再抽出しTable 5の画像も確認。8課題の事前学習重複にはbenchmarkの置換規則。販促×欠品×GBDT比較ではない |

t0公式repoはcommit `a2eca8d80892485c44679c638bcd883ae974f751`へ固定してscaler、mask、variate attention、Transformer呼出側、予測APIを静的に読んだ。causal scalerとfutureの時間方向双方向maskを確認した一方、確認したgroup mask経路には本文のrole別非対称制約を見つけられなかった。論文と実装の差として記録し、学習済みモデルでの漏洩を実証したとはしない。quantile外挿も論文のalpha通常推論と確認時APIで説明が異なる。

最終suiteは **159 tests passed**。7本のrun.pyは単体コピー先で再実行してresults.jsonと一致する。追加の確認は、理由を外しても候補IDが変わらないこと、pooling端数の保持と順序消失、author/video-prefixの候補制約、未来targetの依存経路と単調quantile。Netflixの人工真値はfull 0.36 / LOO 0.32 / 差0.04としてJSONと本文を照合した。

Chromeで **7本×360/390/768/1440 px、36選択状態** が通過。本文の横はみ出し、12節、図、keyboard、内部anchor、local link、download原本一致、index掲載、JS無効時の表示を検査した。図の画像もPC/phoneで目視確認。全図は既存のレスポンシブ部品またはLab固有HTMLから生成し、flow+explorerの一律な組合せは使っていない。

日本語lintは7本とroot README・本記録に実行。YouTube Music・MuSeR・HGenPush・root READMEはfindingなし。t0の同一モデル名で始まる文と語彙多様性のinfoは、alpha/betaの区別と定義の一貫性を優先して保持した。最終通読で証拠の範囲、ppと相対%、合成値と著者値を再確認。lintの中間ファイルはrepoへ含めていない。

7本ともMechanism PARTIAL、Performance / Scaling / Production applicability NOT TESTED。オンラインA/B、GPU性能、商用モデル学習は再現していない。実ログ・個別散布点・holdback数値を作らず、確認できない情報は明示した。既存のdirtyなskill 2件と `codex-last-message.txt` は開始時のSHA-256と一致し、Labのcommit対象から除外した。
